{
  "name": "githubUsersExplorer",
  "version": "1.0.0",
  "description": "githubUsersExplorer",
  "main": "app.js",
  "scripts": {
    "postinstall": "npm install --prefix ./angular-src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run build --prefix ./angular-src",
    "start": "node app",
    "start-dev": "nodemon app"
  },
  "dependencies": {
    "body-parser": "*",
    "cors": "*",
    "express": "*",
    "mongoose": "*",
    "oauth2-server": "^2.4.1"
  },
  "author": "samkit5495",
  "devDependencies": {
    "nodemon": "^1.14.7"
  }
}
